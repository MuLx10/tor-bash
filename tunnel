screen -dmS tor bash -c 'tor --HTTPTunnelPort 8008;exec bash'
echo 'unproxy'>>~/.bashrc
kill -9 \$(pidof tor) >/dev/null 2>/dev/null
torify bash -c 'head -n -1 ~/.bashrc > ~/temp ; mv ~/temp ~/.bashrc'
killall screen 2>/dev/null >/dev/null
kill -9 \$(pidof tor) >/dev/null 2>/dev/null
